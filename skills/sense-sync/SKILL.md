---
name: sense-sync
description: Rebuild or update the Sense semantic search index.
---

# Sense Sync

Call the `sense_sync` MCP tool to update the search index. Display the sync statistics when complete.

If `$ARGUMENTS` is "status" or empty, call `sense_status` instead to show current index coverage without re-indexing.
