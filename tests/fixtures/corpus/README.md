# Fixture Corpus

Minimal sample files used by the Sense test suite.

This corpus contains known files with predictable classifications
so tests can assert on source_type, project, and decay behaviour
without touching a real indexed codebase.
